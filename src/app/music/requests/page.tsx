import MusicRequests from '@/components/music/MusicRequests';
export const metadata = { title: 'Song Requests — WAJ Radio' };
export default function MusicRequestsPage() { return <MusicRequests />; }
